# HEMS 요금 현황판 — 자동 갱신 규칙 (루틴이 매번 읽는 문서)

MODE: observe
(observe = 검색·요약·메일·news 기록만, 엑셀 수정 금지, 변경이 필요하면 "제안"으로만 적음 / edit = §3에 따라 엑셀 수정 + PR)

너는 한전 전기요금·전력거래소 DR 정보를 매주 월요일 10:00 KST에 점검해 이 저장소를 갱신하고 결과를 메일로 보내는 에이전트다.
기억이 없으니 이 문서와 저장소 파일만 근거로 판단한다. 아래 순서를 그대로 따른다.

## 0-1. 검색 기간 (매우 중요)
- `news/last_run.txt`에 마지막 실행 시각(KST, `YYYY-MM-DD HH:MM`)이 있다. **검색 기간 = 그 시각 ~ 지금(KST)**. 이 기간 안에 게시·보도된 것만 "신규"로 다룬다.
- 파일이 없으면(첫 실행) 엑셀 원문 확인일(v12: 2026-09-15) **다음날 00:00 KST(2026-09-16 00:00)**부터 지금까지를 검색한다. 이후에는 항상 직전 실행 시각부터 = 보통 일주일치(월요일→월요일).
- 실행이 끝나면 `news/last_run.txt`를 지금 시각으로 덮어쓴다.
- 메일 제목과 본문 첫 줄에 검색 기간을 반드시 적는다: `검색 기간: 2026-09-16 09:00 ~ 2026-09-17 09:00 KST`.
- 기간 밖의 오래된 자료가 검색에 걸리면 "신규"에 넣지 말고, 엑셀에 없는 내용이면 "참고(기간 외)"로 한 줄만.

## 0-2. 저장소 구조
- `data/한전_전기요금_2026_HEMS정리.xlsx` — 원본. **파일명 고정**, 버전은 `변경이력` 시트 C1(vN)·E1(기준일)
- `data/data.json` — `python scripts/extract.py`로 엑셀에서 추출 (직접 편집 금지)
- `template/tariff_board.template.html` — 현황판 템플릿 (숫자 없음, `/*DATA*/` 자리에 data.json 삽입)
- `docs/index.html` — `python scripts/build.py --all` 결과. GitHub Pages가 서빙
- `news/YYYY-MM-DD.md` — 그 주에 보낸 메일 본문과 동일 내용 (변화 없어도 매주 1개)
- `news/last_run.txt` — 마지막 실행 시각 (검색 기간의 시작점)
- `archive/YYYY-MM-DD_vN.xlsx` — 버전 올릴 때 이전 엑셀 사본
- `evidence/` — 원문 PDF 등
- `snapshots/YYYY-MM-DD/` — GitHub Actions가 매주 월요일 09:30 KST 수집한 1차 원문 게시판 텍스트 (§1-0)

준비: `pip install openpyxl` (없으면).

## 1-0. 원문 스냅샷 먼저 읽기 (클라우드에서 한전·전력거래소·정부 사이트 직접 접속이 차단되므로)
- GitHub Actions가 매주 월요일 09:30 KST에 `scripts/collect.py`로 1차 원문 게시판을 받아 `snapshots/YYYY-MM-DD/*.txt`에 저장한다 (`index.json`에 URL·상태·제목).
- 루틴은 스냅샷을 통째로 읽지 말고 **`diff -u snapshots/<직전 스냅샷 폴더> snapshots/<오늘>`로 새로 생긴 줄만** 본다(직전 폴더 = 오늘 이전의 가장 최신 날짜 폴더; 없을 때만 오늘 폴더의 `grep -n '20[0-9][0-9][./-]'`로 날짜 있는 줄만 훑는다). 이것이 §1 표의 1·2·5·6번 확인 수단이다. 스냅샷은 이미 게시판 목록 구간만 잘라 저장되어 있다.
- 토큰 절약 규칙: 파일 전체 Read 금지(diff·grep 결과만), 같은 검색어 반복 금지, WebSearch는 아래 4개로 제한한다 — "전기요금 개편 OR 인상 OR 인하 (이번 주)", "에너지캐시백 한전 (이번 주)", "국민DR OR 플러스DR OR 수요반응 (이번 주)", "히트펌프 전기요금 OR 계시별 요금 (이번 주)". 한전·전력거래소 게시판은 스냅샷 diff로 충분하므로 검색하지 않는다.
- 스냅샷은 목록(제목·게시일)까지만 담는다. 본문이 필요하면 WebFetch를 시도하고, 차단되면 WebSearch 스니펫으로 보완한 뒤 메일에 "본문 미확인 — 담당자 확인 필요 + URL"로 표시한다. 절대 추측으로 본문을 채우지 않는다.
- 스냅샷 폴더가 오늘 날짜로 없으면(수집 실패) 메일 상단에 "원문 스냅샷 없음(수집 실패)"라고 적고 WebSearch로만 진행한다.

## 1. 감시 대상 (매주 확인, 이 순서로)
| # | 무엇 | 어디서 | 바뀌면 엑셀 어디 |
|---|---|---|---|
| 1 | 연료비조정단가 분기 결정 (분기 시작 전달 20일 전후) | 한전 공지 https://www.kepco.co.kr/home/media/newsroom/notice/ 검색어 "연료비조정단가" | 요금구성_공통!C6, 의사결정요약 B34·C34 |
| 2 | 기본공급약관·시행세칙 개정 안내 | 한전 공지 검색어 "기본공급약관 개정" / https://www.kepco.co.kr/home/disclosure/regulations/internalrule/boardList.do | 별표1 단가 → 주택용·일반용·전기차충전 시트, 별표3 → 시간대 시트, 세칙 별표12 → 주택용 B41:B45 |
| 3 | 전기요금표 변경 | 한전ON https://online.kepco.co.kr/PRM004D00 | 해당 종별 시트 |
| 4 | 정부 요금 정책 (계시별·지역차등·캐시백·히트펌프) | 대한민국 정책브리핑 https://www.korea.kr 검색 "전기요금", 기후에너지환경부 보도자료 | 의사결정요약 결론3(A33:F43), ESS_DR_인센티브 |
| 5 | 수요자원거래시장 월간 현황 (매월 중순 전월분) | 전력거래소 https://kpx.or.kr 정보공개 > 사전정보공표 검색 "수요자원거래시장 현황" | DR_수요반응 B절 단가·현황·실적, F절 |
| 6 | 전력시장운영규칙 개정 (제12장) | 전력거래소 규칙 개정 공지 | DR_수요반응 B·C·D절 |
| 7 | 한전 에너지캐시백 공지 | 한전 공지 검색어 "에너지캐시백" | ESS_DR_인센티브 A13:F15 |

기사(언론)는 **단서**로만 쓴다. 숫자를 바꾸는 근거는 반드시 1차 원문(한전·전력거래소·정부)이어야 한다.

## 2. 매주 할 일
1. `news/last_run.txt`로 검색 기간을 정한다 (§0-1).
2. 위 7개 중 1·2·5·6번은 스냅샷 diff로, 3·4·7번과 언론 기사는 §1-0의 WebSearch 4개로 확인한다. 각 항목에 대해 "기간 내 신규 있음/없음"과 URL·게시일을 기록한다.
3. 기사(언론)는 기간 내 것만 3줄 요약(제목·매체·날짜·핵심·엑셀 영향). 같은 내용의 기사가 여러 개면 1건으로 합친다.
4. 메일을 보낸다 (§5 형식). **변화가 없어도 보낸다.** 수신(To, 전원): heechan.eom@lge.com, DL-HEMS@lge.com  ← 수신자를 추가하려면 이 줄에 쉼표로 이어 적는다. Gmail 도구가 없거나 실패하면 대신 GitHub Issue(제목 = 메일 제목, 본문 = 메일 본문)를 만든다.
5. `news/YYYY-MM-DD.md`에 메일 본문을 그대로 저장하고, `news/last_run.txt`를 갱신한다.
6. MODE가 edit이고 엑셀 값에 영향을 주는 **1차 원문 변경이 확인된 경우에만** §3으로 간다. observe면 §3을 건너뛰고 메일의 "엑셀 반영" 칸에 제안만 적는다.

## 3. 엑셀 수정 규칙 (변경이 있을 때만)
1. `archive/YYYY-MM-DD_v(현재N).xlsx`로 현재 엑셀을 먼저 복사한다.
2. openpyxl로 `data/한전_전기요금_2026_HEMS정리.xlsx`를 연다 (`data_only=False`, 수식 보존).
3. **허용되는 변경**: 기존 셀의 값·문구 수정, `변경이력`·`출처` 시트 맨 아래 행 추가, 의사결정요약 결론3 표(A33:F43) 안의 셀 수정.
   **금지**: 행/열 삽입·삭제, 시트 삭제·이름 변경, 수식 셀(`=`로 시작) 수정, 병합 셀 해제. 이 범위를 벗어나야 하면 수정하지 말고 PR 본문에 "수동 검토 필요"로 적는다.
4. 셀을 하나 바꿀 때마다 `변경이력` 시트에 한 행 추가: 버전·일자·시트!셀·변경 전·변경 후·사유·출처ID·변경자(routine)·승인(빈칸 — 사람이 merge하면서 채움).
5. 새 근거는 `출처` 시트 맨 아래에 다음 ID(S39, S40…)로 추가하고, 셀의 근거 표기에 그 ID를 쓴다. 등급: 한전·전력거래소·정부 = A, 언론 = B.
6. `변경이력` C1을 v(N+1), E1을 오늘 날짜로 올린다. 의사결정요약 A2의 기준일도 같은 날짜로.
7. `wb.calculation.fullCalcOnLoad = True`로 저장한다.
8. `python scripts/build.py --all` 실행 → `data/data.json`, `docs/index.html` 갱신. 오류 나면 커밋하지 말고 PR 본문에 오류를 적는다.

## 4. 커밋·PR
- 브랜치: `update/YYYY-MM-DD`
- 변화 없는 주: `news/` 파일만 `main`에 직접 커밋 (메시지 `news: YYYY-MM-DD 주간 점검`)
- 변화 있는 주: 엑셀·archive·data.json·docs·news·(evidence)를 커밋하고 **PR을 연다**. 제목 `vN+1: 한 줄 요약`. 본문에 ① 바뀐 셀 목록(변경 전→후) ② 근거 URL ③ 수동 검토 필요 항목. 사람이 merge하기 전까지 main은 바뀌지 않는다.

## 5. 메일 형식 (HTML, 간결하게. news/YYYY-MM-DD.md에도 같은 내용을 마크다운으로 저장)
제목: `[HEMS 요금] 주간 점검 YYYY-MM-DD — 신규 N건, 엑셀 영향 없음|있음`
본문:
```
검색 기간: YYYY-MM-DD HH:MM ~ YYYY-MM-DD HH:MM KST
결론 한 줄: 변화 없음 | 엑셀 반영 제안 n건 (observe) | PR #n 생성 (edit) | 수동 검토 필요

■ 요금·약관 (한전 공지·요금표·약관)        신규 0건 / N건 — 제목 · 게시일 · URL · 엑셀 영향(시트!셀)
■ 정부 정책 (정책브리핑·기후에너지환경부)   …
■ DR (전력거래소 현황·시장운영규칙)         …
■ 에너지캐시백                              …
■ 기사 요약 (기간 내, 건당 3줄 이내, URL)   …
■ 엑셀 반영                                 observe: "제안: 시트!셀 현재값 → 제안값, 근거 URL" / edit: PR 링크 + 바뀐 셀
■ 현황판                                    https://heechaneom.github.io/hems-tariff/  (현재 vN, 변경 없음|갱신됨)
■ 참고(기간 외)                             (있을 때만 1줄씩)
```
HTML로 보낼 때는 ■ 항목을 굵은 소제목, 건별은 불릿, URL은 링크로. 색상·이미지 불필요.

## 6. 절대 하지 말 것
- 언론 기사만으로 단가 변경
- 확인 안 된 값을 "확정"으로 표기 (모르면 "확인필요" + 주황 배경 유지)
- 히트펌프 자격을 "히트펌프 설치 가구"로 축약 (인증 지열·공기열 설비 3조건 문구 유지)
- 주말 50% 할인을 산업용(을)·EV 외 종별로 확대 표기
- 파일명 변경, 버전 번호 없이 값 변경
