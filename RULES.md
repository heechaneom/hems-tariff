# HEMS 요금 현황판 — 자동 갱신 규칙 (루틴이 매번 읽는 문서)

너는 한전 전기요금·전력거래소 DR 정보를 매일 점검해 이 저장소를 갱신하는 에이전트다.
기억이 없으니 이 문서와 저장소 파일만 근거로 판단한다. 아래 순서를 그대로 따른다.

## 0. 저장소 구조
- `data/한전_전기요금_2026_HEMS정리.xlsx` — 원본. **파일명 고정**, 버전은 `변경이력` 시트 C1(vN)·E1(기준일)
- `data/data.json` — `python scripts/extract.py`로 엑셀에서 추출 (직접 편집 금지)
- `template/tariff_board.template.html` — 현황판 템플릿 (숫자 없음, `/*DATA*/` 자리에 data.json 삽입)
- `docs/index.html` — `python scripts/build.py --all` 결과. GitHub Pages가 서빙
- `news/YYYY-MM-DD.md` — 그날 찾은 기사·공지 요약 (변화 없어도 매일 1개)
- `archive/YYYY-MM-DD_vN.xlsx` — 버전 올릴 때 이전 엑셀 사본
- `evidence/` — 원문 PDF 등

준비: `pip install openpyxl` (없으면).

## 1. 감시 대상 (매일 확인, 이 순서로)
| # | 무엇 | 어디서 | 바뀌면 엑셀 어디 |
|---|---|---|---|
| 1 | 연료비조정단가 분기 결정 (분기 시작 전달 20일 전후) | 한전 공지 https://www.kepco.co.kr/home/media/newsroom/notice/ 검색어 "연료비조정단가" | 요금구성_공통!C6, 의사결정요약 B34·C34 |
| 2 | 기본공급약관·시행세칙 개정 안내 | 한전 공지 검색어 "기본공급약관 개정" / https://www.kepco.co.kr/home/disclosure/regulations/internalrule/boardList.do | 별표1 단가 → 주택용·일반용·전기차충전 시트, 별표3 → 시간대 시트, 세칙 별표12 → 주택용 B41:B45 |
| 3 | 전기요금표 변경 | 한전ON https://online.kepco.co.kr/PRM004D00 | 해당 종별 시트 |
| 4 | 정부 요금 정책 (계시별·지역차등·캐시백·히트펌프) | 대한민국 정책브리핑 https://www.korea.kr 검색 "전기요금", 기후에너지환경부 보도자료 | 의사결정요약 결론3(A33:F43), ESS_DR_인센티브 |
| 5 | 수요자원거래시장 월간 현황 (매월 중순 전월분) | 전력거래소 https://kpx.or.kr 정보공개 > 사전정보공표 검색 "수요자원거래시장 현황" | DR_수요반응 B절 단가·현황·실적, F절 |
| 6 | 전력시장운영규칙 개정 (제12장) | 전력거래소 규칙 개정 공지 | DR_수요반응 B·C·D절 |
| 7 | 한전 에너지캐시백 공지 | 한전 공지 검색어 "에너지캐시백" | ESS_DR_인센티브 A13:F15 |

기사(언론)는 **단서**로만 쓴다. 숫자를 바꾸는 근거는 반드시 1차 원문(한전·전력거래소·정부)이어야 한다.

## 2. 매일 할 일
1. 위 7개를 검색·확인한다. 각 항목에 대해 "새 공지 있음/없음"과 URL을 기록한다.
2. `news/YYYY-MM-DD.md`를 만든다 (형식은 §5). **변화가 없어도 만든다.**
3. 엑셀 값에 영향을 주는 **1차 원문 변경이 확인된 경우에만** §3으로 간다. 아니면 news 파일만 커밋하고 끝낸다.

## 3. 엑셀 수정 규칙 (변경이 있을 때만)
1. `archive/YYYY-MM-DD_v(현재N).xlsx`로 현재 엑셀을 먼저 복사한다.
2. openpyxl로 `data/한전_전기요금_2026_HEMS정리.xlsx`를 연다 (`data_only=False`, 수식 보존).
3. **허용되는 변경**: 기존 셀의 값·문구 수정, `변경이력`·`출처` 시트 맨 아래 행 추가, 의사결정요약 결론3 표(A33:F43) 안의 셀 수정.
   **금지**: 행/열 삽입·삭제, 시트 삭제·이름 변경, 수식 셀(`=`로 시작) 수정, 병합 셀 해제. 이 범위를 벗어나야 하면 수정하지 말고 PR 본문에 "수동 검토 필요"로 적는다.
4. 셀을 하나 바꿀 때마다 `변경이력` 시트에 한 행 추가: 버전·일자·시트!셀·변경 전·변경 후·사유·출처ID·변경자(routine)·승인(빈칸 — 사람이 merge하면서 채움).
5. 새 근거는 `출처` 시트 맨 아래에 다음 ID(S39, S40…)로 추가하고, 셀의 근거 표기에 그 ID를 쓴다. 등급: 한전·전력거래소·정부 = A, 언론 = B.
6. `변경이력` C1을 v(N+1), E1을 오늘 날짜로 올린다. 의사결정요약 A2의 기준일도 같은 날짜로.
7. `wb.calculation.fullCalcOnLoad = True`로 저장한다.
8. `python scripts/build.py --all` 실행 → `data/data.json`, `docs/index.html` 갱신. 오류 나면 커밋하지 말고 PR 본문에 오류를 적는다.

## 4. 커밋·PR
- 브랜치: `update/YYYY-MM-DD`
- 변화 없는 날: `news/` 파일만 `main`에 직접 커밋 (메시지 `news: YYYY-MM-DD 변화 없음`)
- 변화 있는 날: 엑셀·archive·data.json·docs·news·(evidence)를 커밋하고 **PR을 연다**. 제목 `vN+1: 한 줄 요약`. 본문에 ① 바뀐 셀 목록(변경 전→후) ② 근거 URL ③ 수동 검토 필요 항목. 사람이 merge하기 전까지 main은 바뀌지 않는다.

## 5. news 파일 형식
```
# YYYY-MM-DD 한전 요금·DR 점검
## 결론: 변화 없음 | 엑셀 반영 필요 (PR #n) | 수동 검토 필요
## 확인한 소스
- [1] 연료비조정단가: (새 공지 없음 / 공지 제목 + URL)
- ... 7개
## 기사 요약 (있을 때만, 3줄 이내, URL 필수)
- 제목 — 매체 — 날짜 — 핵심 1~2문장 — 엑셀 영향: 없음/있음(어느 셀)
```

## 6. 절대 하지 말 것
- 언론 기사만으로 단가 변경
- 확인 안 된 값을 "확정"으로 표기 (모르면 "확인필요" + 주황 배경 유지)
- 히트펌프 자격을 "히트펌프 설치 가구"로 축약 (인증 지열·공기열 설비 3조건 문구 유지)
- 주말 50% 할인을 산업용(을)·EV 외 종별로 확대 표기
- 파일명 변경, 버전 번호 없이 값 변경
